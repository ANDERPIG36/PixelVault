@page
@model PixelVault.Pages.Publishers.IndexModel
@{
    ViewData["Title"] = "Gestione Editori";
}

<div class="row">
    <div class="col-md-5">
        <div class="card shadow-sm mb-4">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">➕ Aggiungi Editore</h5>
            </div>
            <div class="card-body">
                <form method="post">
                    <div class="mb-3">
                        <label class="form-label font-weight-bold">Nome Editore *</label>
                        <input asp-for="NewPublisher.Name" class="form-control" placeholder="Es. Sony Interactive, Bandai Namco..." required />
                    </div>
                    <button type="submit" class="btn btn-success w-100">Salva Editore</button>
                </form>
            </div>
        </div>
    </div>

    <div class="col-md-7">
        <div class="card shadow-sm">
            <div class="card-header bg-dark text-white">
                <h5 class="mb-0">🏢 Editori Esistenti</h5>
            </div>
            <div class="card-body p-0">
                <ul class="list-group list-group-flush">
                    @if (!Model.Publishers.Any()) {
                        <li class="list-group-item text-muted text-center py-3">Nessun editore presente.</li>
                    }
                    else {
                        @foreach (var publisher in Model.Publishers) {
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <span class="fw-bold">@publisher.Name</span>
                                <div>
                                    <span class="badge bg-secondary rounded-pill me-2">ID: @publisher.Id</span>
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@publisher.Id" onsubmit="return confirm('Confermi l\'eliminazione dell\'editore?');" style="display:inline;">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Rimuovi</button>
                                    </form>
                                </div>
                            </li>
                        }
                    }
                </ul>
            </div>
        </div>
    </div>
</div>